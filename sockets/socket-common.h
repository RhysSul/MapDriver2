/*
    Define a common header file where common protocol messages (see below) are defined as well as
    the default port (23032) and IP (127.0.0.1). Allow IP override via argv (on both client and server programs).
*/

#define PORT 23032
#define IP "127.0.0.1"
